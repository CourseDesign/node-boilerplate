{
  "name": "template-neon",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "run": "node dist/index.js",
    "build": "neon build && gulp",
    "test": "jest",
    "install": "npm run build && npm run test"
  },
  "lint-staged": {
    "src/*.{js,ts}": [
      "eslint --fix"
    ]
  },
  "jest": {
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testRegex": "\\.spec\\.ts$",
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "globals": {
      "ts-jest": {
        "diagnostics": true
      }
    }
  }
}
